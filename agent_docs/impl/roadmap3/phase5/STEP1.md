# STEP 1 — Phase Contract and Targets

## 진행 상태

- [x] Separate semantic `arc` mark boundary
- [x] Donut aggregate angular-partition grain
- [x] Rose equal-band overlay grain
- [x] Radial-bar equal-band radial-extent grain
- [x] Exact target call chains and hard Gate boundary
- [x] Radial title inside default and explicit outside option

Phase 5는 pie, donut, rose, radial bar를 별도 chart compiler나 facade로 만들지 않는다. Arc mark와 existing
theta/radius/color encodings가 데이터 grain에 따라 final sectors를 materialize한다. `innerRadius`와 `padAngle`은
mark-level graphical intent이며 color `layout: "overlay"`만 rose series의 겹침 의미를 명시한다.

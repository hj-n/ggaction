# Step 2 — D-001 fragment offset

## 진행 상태

- [ ] Shared CSS custom property 기반 h2/h3 `scroll-margin-top`
- [ ] TOC current-section 계산이 같은 computed offset 사용
- [ ] Direct hash, permalink와 TOC click regression
- [ ] Desktop/mobile와 no-JavaScript compatibility

Topbar 높이를 CSS와 JavaScript에 별도 숫자로 중복하지 않는다.

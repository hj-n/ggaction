# Step 4 — Public action, state와 rematerialization

## 진행 상태

- [ ] text target inference와 complete preflight
- [ ] `layoutLabels` wrapped materialization hierarchy
- [ ] `removeLabelLayout` base-position restoration과 leader cleanup
- [ ] text/font/encoding/data/scale/Canvas replay plan
- [ ] immutable prior-program, trace와 stable warning assertions

Every replay rematerializes semantic base text first and applies label layout exactly once afterward.

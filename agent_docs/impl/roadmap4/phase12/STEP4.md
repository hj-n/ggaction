# Step 4 — Public action, state와 rematerialization

## 진행 상태

- [x] text target inference와 complete preflight
- [x] `layoutLabels` wrapped materialization hierarchy
- [x] `removeLabelLayout` base-position restoration과 leader cleanup
- [x] text/font/encoding/data/scale/Canvas replay plan
- [x] immutable prior-program, trace와 stable warning assertions

Every replay rematerializes semantic base text first and applies label layout exactly once afterward.

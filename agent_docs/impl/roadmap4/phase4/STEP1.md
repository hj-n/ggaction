# Step 1 — Jitter contract와 independent oracle

## 진행 상태

- [x] `jitterPoints`와 `removeJitter` lifecycle 결정
- [x] pixel/band bound와 effective category slot 정의
- [x] key/seed canonical identity와 `jitter-uniform-v1` 정의
- [x] semantic/materialization/graphic owner 분리
- [x] fixed hash vector와 containment oracle

P4-A 승인 전 후보 API는 Phase-local proposal에만 둔다. `ACTION_INDEX`, runtime prototype와 public
declaration에는 추가하지 않는다.

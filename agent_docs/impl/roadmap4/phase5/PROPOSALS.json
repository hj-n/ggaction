{
  "version": 1,
  "phase": "roadmap4/phase5",
  "gate": "P5-A",
  "status": "approved",
  "actions": [
    {
      "name": "createWindowData",
      "status": "implemented",
      "required": ["id", "operations"],
      "optional": ["source", "partitionBy", "sortBy"],
      "replacement": "immutable create-only; a duplicate id is rejected without changing consumers",
      "stateOwner": "named derived dataset and canonical transform registry"
    },
    {
      "name": "createBin2DData",
      "status": "proposed",
      "required": ["id", "x", "y"],
      "optional": ["source", "bins", "extent", "includeEmpty", "members", "as"],
      "replacement": "recreating the same derived id replaces its revision and rematerializes consumers",
      "stateOwner": "named derived dataset and canonical transform registry"
    },
    {
      "name": "createHeatmap",
      "status": "proposed-extension",
      "required": ["x", "y"],
      "optional": ["id", "data", "coordinate", "bin", "color", "rect", "guides"],
      "replacement": "bin omitted preserves the implemented pre-gridded contract",
      "stateOwner": "wrapped derived dataset, rect layer, encodings, scales and guides"
    }
  ]
}
